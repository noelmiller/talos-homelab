apiVersion: v1
kind: Namespace
metadata:
  name: dashboard
  # Homepage's image runs as root, so baseline is the ceiling until that changes.
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
