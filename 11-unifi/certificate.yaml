apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: unifi-k8s-noelmiller-dev
  namespace: unifi
spec:
  secretName: unifi-k8s-noelmiller-dev-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - unifi.k8s.noelmiller.dev
