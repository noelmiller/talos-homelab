apiVersion: v1
kind: Namespace
metadata:
  name: coder
  # baseline rather than restricted so workspace templates keep room to add capabilities.
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
